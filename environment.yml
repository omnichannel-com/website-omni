name: website-omni
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.14
  # NOTE: Per constitution §Technology Standards, `uv` is the preferred
  # Python dependency manager. Use `uv` (or `uv pip`) for any Python
  # packages; do not add `pip` dependencies here.
  #
  # Activate this environment with:
  #   source /opt/anaconda3/bin/activate website-omni
