# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: calculators.spec.ts >> Calculators >> Queueing Theory Explainer loads and has brand styling
- Location: e2e/calculators.spec.ts:10:9

# Error details

```
Error: page.goto: net::ERR_CONNECTION_REFUSED at http://localhost:3000/calculator/queueing-theory-explainer.html
Call log:
  - navigating to "http://localhost:3000/calculator/queueing-theory-explainer.html", waiting until "load"

```

# Test source

```ts
  1  | import { test, expect } from "@playwright/test";
  2  | 
  3  | const CALCULATORS = [
  4  |   { path: "/calculator/genesys-shrinkage-calculator.html", name: "Genesys Shrinkage Calculator" },
  5  |   { path: "/calculator/queueing-theory-explainer.html", name: "Queueing Theory Explainer" },
  6  | ];
  7  | 
  8  | test.describe("Calculators", () => {
  9  |   for (const { path, name } of CALCULATORS) {
  10 |     test(`${name} loads and has brand styling`, async ({ page }) => {
> 11 |       await page.goto(path);
     |                  ^ Error: page.goto: net::ERR_CONNECTION_REFUSED at http://localhost:3000/calculator/queueing-theory-explainer.html
  12 |       await expect(page.locator("body")).toBeVisible();
  13 | 
  14 |       // No old brand references
  15 |       const bodyText = await page.locator("body").innerText();
  16 |       expect(bodyText.toLowerCase()).not.toContain("2ai");
  17 |       expect(bodyText.toLowerCase()).not.toContain("alterego");
  18 |     });
  19 |   }
  20 | 
  21 |   test("shrinkage calculator produces expected output", async ({ page }) => {
  22 |     await page.goto("/calculator/genesys-shrinkage-calculator.html");
  23 | 
  24 |     // Find and interact with a basic input if present
  25 |     const inputs = page.locator("input");
  26 |     const count = await inputs.count();
  27 |     if (count > 0) {
  28 |       await expect(inputs.first()).toBeVisible();
  29 |     }
  30 | 
  31 |     // Verify some output element exists
  32 |     const outputs = page.locator("output, .result, #result, .output");
  33 |     const outputCount = await outputs.count();
  34 |     if (outputCount > 0) {
  35 |       await expect(outputs.first()).toBeVisible();
  36 |     }
  37 |   });
  38 | });
  39 | 
```